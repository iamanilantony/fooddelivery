import { Button, Grid, ListItem, ListItemText, Typography } from "@material-ui/core";
import { Box, styled } from "@mui/system";
import { Colors } from './theme';

export const ListContainer = styled(Grid)((theme)=>({

}))
export const ListTitles = styled(Typography)((theme)=>({
    
}))
export const Lists = styled(ListItem)(()=>({

}))
export const ListItems = styled(ListItemText)(()=>({

}))
export const ListImg = styled("img")(()=>({
    
}))
export const ListTitle = styled(Typography)((theme)=>({
    
}))
export const ListInfo = styled(Typography)((theme)=>({
    
}))
export const ListDesc = styled(Typography)((theme)=>({
    
}))
export const ListButton = styled(Button)(()=>({
    
}))

